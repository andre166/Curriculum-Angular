import { WrapperCertificadosComponents } from './wrapper-certificados.components';

describe('WrapperCertificados.Components', () => {
  it('should create an instance', () => {
    expect(new WrapperCertificadosComponents()).toBeTruthy();
  });
});
