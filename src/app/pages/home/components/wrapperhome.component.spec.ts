import { WrapperhomeComponent } from './wrapperhome.component';

describe('Wrapperhome.Component', () => {
  it('should create an instance', () => {
    expect(new WrapperhomeComponent()).toBeTruthy();
  });
});
