import { ContatoRoutingModule } from './contato-routing.module';

describe('ContatoRouting.Module', () => {
  it('should create an instance', () => {
    expect(new ContatoRoutingModule()).toBeTruthy();
  });
});
